import { createRoot } from "react-dom/client";
import ExtensionPopup from "./components/ExtensionPopup";
import "./index.css"; // Important: Import your styles

createRoot(document.getElementById("root")!).render(<ExtensionPopup />);